services:
  - type: web
    name: expense-tracker-api
    runtime: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
